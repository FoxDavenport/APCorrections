
/**
 * Write a description of class Unit6_Q11 here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class Unit6_Q11
{
    public static void main(String[]args){
        
        boolean duplicates = false;
for (int x = 0; x < arr.length - 1; x++)
{
/* missing loop header */
{
if (arr[x] == arr[y])
{
duplicates = true;
}
}
}
}
//looking to replace missing loop header to get code to run.
//want duplicates to be true if there is duplicate in arr
//want false if no duplicates

//for (int y = x + 1; y < arr.length; y++)
//This is the correct answer choice as the outer loop will iterate through
//all elements of the array. This one will examine all elements that come
//after current element. Make sure to not check itself. If duplicate then true
//If no duplicate then false
}

